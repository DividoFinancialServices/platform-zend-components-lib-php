# platform-zend-components-lib-php
These components are extracted from `platform`, and are extracted here so that they can be imported by Composer in the usual way instead of having to work around them.
